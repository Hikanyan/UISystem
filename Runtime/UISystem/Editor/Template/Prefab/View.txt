using UnityEngine;

namespace {{NAMESPACE}}
{
    /// <summary>
    /// {{BASE_NAME}} View
    /// Prefab UI は SceneUIRegistrar を要求しない（Scene 固有管理に寄せない）前提。
    /// </summary>
    [DisallowMultipleComponent]
    [RequireComponent(typeof({{PRESENTER_CLASS}}))]
    public sealed class {{VIEW_CLASS}} : MonoBehaviour
    {
        // Viewの参照は必要に応じて追加してください
        // [SerializeField] private UnityEngine.UI.Button closeButton;

        public void SetMessage(string message)
        {
            // 必要に応じてUIへ反映
            // Debug.Log(message);
        }
    }
}
