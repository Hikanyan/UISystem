using TMPro;
using UnityEngine;

namespace {{NAMESPACE}}
{
    [DisallowMultipleComponent]
    [RequireComponent(typeof(SceneUIRegistrar))]
    [RequireComponent(typeof({{PRESENTER_CLASS}}))]
    public class {{VIEW_CLASS}} : MonoBehaviour
    {
        [SerializeField] private TextMeshProUGUI text;

        public void SetMessage(string message)
        {
            if (text != null)
            {
                text.text = message;
            }
        }
    }
}
